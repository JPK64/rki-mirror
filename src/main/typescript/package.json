{
  "name": "typescript",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:8080/",
  "dependencies": {
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^13.1.0",
    "@types/jest": "^26.0.15",
    "@types/node": "^15.0.0",
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0",
    "browserslist": "^4.16.5",
    "node-sass": "^6.0.0",
    "postcss": "^8.2.10",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3",
    "typescript": "^4.1.2",
    "web-vitals": "^2.0.1"
  },
  "config": {
    "resourceDir": "../../../target/classes/META-INF/resources"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build --dest && mkdir -p ${npm_package_config_resourceDir} && rsync -a build/* ${npm_package_config_resourceDir}",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "clean": "rm -rf build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
